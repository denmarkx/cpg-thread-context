package utils;

import de.fraunhofer.aisec.cpg.graph.Node;

public class NodeUtil {

    /*
     * Replaces the given node's type with a new type.
    */
    public static void replace(Node node) {
    }
}
